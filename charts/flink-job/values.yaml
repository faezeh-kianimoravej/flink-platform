tenantName: demo-tenant
namespace: demo-tenant

image:
  repository: ghcr.io/faezeh-kianimoravej/flink-demo-job
  # GitOps deployments should pin this to the immutable tenant CI image tag
  # produced from github.sha, for example: 3f1c392f408a9a3813ef3b9fc3df134e0c8393f9.
  tag: TODO_REPLACE_WITH_TENANT_COMMIT_SHA
  pullPolicy: IfNotPresent

flinkVersion: v2_2

serviceAccount: flink

jobManager:
  resource:
    cpu: 1
    memory: "1024m"

taskManager:
  replicas: 1
  resource:
    cpu: 1
    memory: "1024m"

job:
  className: com.example.flink.DemoJob
  jarURI: local:///opt/flink/usrlib/job.jar
  parallelism: 1
  restartNonce: 1
  upgradeMode: stateless

kafka:
  bootstrapServers: flink-platform-kafka-kafka-bootstrap.kafka-system.svc.cluster.local:9092
  inputTopics:
    - demo-orders
    - demo-customers
  outputTopic: demo-enriched-orders
  consumerGroupId: demo-flink-job

checkpointing:
  interval: 30s
